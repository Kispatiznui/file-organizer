[project]
name = "file-organizer-cli"
version = "0.2.0"
description = "CLI tool for file organization and cleanup"
authors = [{name = "Luis Felipe Martinez"}]
readme = "README.md"
requires-python = ">=3.8"

[project.scripts]
file-organizer = "main:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
